[project]
name = "bank-masks"
version = "0.1.0"
description = ""
authors = [
    {name = "Dmitriy",email = "zora5891@rambler.ru"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy (>=1.18.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.lint.dependencies]
flake8 = "^7.3.0"
black = "^25.9.0"
isort = "^7.0.0"
mypy = "^1.18.2"



[tool.poetry.group.dev.dependencies]
pytest = "^9.0.0"
pytest-cov = "^7.0.0"

[tool.black]
line-length = 119
exclude = '''
/\.git
'''

[tool.isort]
line_length = 119
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true

[tool.mypy]
disallow_untyped_defs = true
warn_return_any = true
exclude = [
    "venv",
]